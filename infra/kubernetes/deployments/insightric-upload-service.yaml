apiVersion: apps/v1
kind: Deployment
metadata:
  name: insightric-upload-service
  labels:
    app: insightric-upload-service
spec:
  replicas: 1
  selector:
    matchLabels:
      app: insightric-upload-service
  template:
    metadata:
      labels:
        app: insightric-upload-service
    spec:
      containers:
        - name: insightric-upload-service
          image: ""
          ports: []